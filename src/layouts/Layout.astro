---
import Nav from "@components/Nav.astro";
import 	"@styles/global.css";
import Head from "src/share/components/head/Head.astro";
import {envSrcUrl, envLinkUrl} from "../share/components/head/env";
const {title} = Astro.props;
const data = Astro.locals;
console.log(data)



---	

<!doctype html>
<html lang="ko">
	<Head
		title={title}
	/>	
	<body>
		<div class="wrap">
			<header>
				<div class="logo">
					<a href={envLinkUrl("/")}>
					<picture>
						<source media="(max-width : 768px)" srcset={envSrcUrl("logo2.png")}>
						<source media="(min-width : 769px)" srcset={envSrcUrl("logo.png")}>
						<img src={envSrcUrl("logo.png")} width="100px" alt="nike">
					</picture>		
					</a>		
				</div>
				<Nav />
				
			</header>
			<slot />	
			
			<footer>			
				<!-- {locals} -->
			</footer>

		</div>
	</body>
</html>

<style lang="scss">
	// @use "../share/style/variable.scss" as *;
	.wrap {
		width: 1200px;
		margin: 0 auto;
	}
	header {
		display: flex;
		gap: 12px;
		align-items: center;
	}
	
	
	@media all and (max-width: 768px) {
		.wrap {
			padding: 0 24px;
			width: calc(100% - 48px);

		}
	}
	@media all and (min-width: 769px) and (max-width: 1024px) {

	}
	@media all and (min-width: 1025px) {
		
	}

</style>
<style is:global lang="scss">
	:root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );

    --background: #eee;
    --text-color: #2c2c2c;
  }

  .dark {
    --background: #303b57;
    --text-color: #fff;
  }

  html {
    font-family: system-ui, sans-serif;
    background: var(--background);
    color: var(--text-color);

  }
</style>